# Acquire the plotting theme from the environment

If \`theme_onco()\` is not available, returns a generic ggplot minimal
theme.

## Usage

``` r
.apply_onco_plot_theme(base_size = 12)
```

## Arguments

- base_size:

  Base font size.

## Value

A ggplot theme.
