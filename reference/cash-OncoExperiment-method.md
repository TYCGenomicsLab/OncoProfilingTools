# Extract a column from OncoExperiment metadata

This is a convenience accessor that forwards \`\$\` lookups to the
top-level \`colData()\` stored on the \`OncoExperiment\`.

## Usage

``` r
# S4 method for class 'OncoExperiment'
x$name
```
